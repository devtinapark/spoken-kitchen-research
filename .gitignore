# secrets — never commit
.env
.env.*
*.key

# node
node_modules/

# python
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store

# Raw Nimble output (data/raw/) is committed so readers can see the actual
# responses behind every finding without needing an API key.
